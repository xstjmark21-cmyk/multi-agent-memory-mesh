# PRIVACY_RULES —— 全局隐私红线（示例模板）

> **本文件是 mesh 内所有 agent 必读的第一份文件。**
> 任何信息写入本地记忆、共享中心、笔记系统之前，先过一遍本规则。
> 原则：**入库前拦截，而不是入库后清理**（清理漏一处就等于永久泄露）。

## 1. 绝对禁止入库（Never Store）

| 类别 | 说明 | 替代做法 |
|------|------|---------|
| 真实姓名 | 用户及家人真实姓名（含拼音、外文写法、缩写） | 统一使用安全代称，如 `用户` / `Owner` |
| 认证凭据 | 密码、私钥、API Key、Token、订阅链接、恢复码 | 只留在密码管理器；记忆里只写"凭据存放位置" |
| 支付信息 | 银行卡号、支付账号、账单明细 | 不记录 |
| 身份证件 | 证件号、护照号、社保号、住址门牌 | 不记录 |
| 精确位置 | 家庭/公司门牌级地址、实时定位 | 只写城市级或代号 |
| 他人隐私 | 第三方的联系方式、私聊内容 | 不记录 |

## 2. 谨慎入库（Store With Care）

- **主机名 / 内网 IP / 端口**：内部记忆可存（运维必需），但**禁止随记忆外发或写入公开仓库**
- **域名 / 邮箱**：只在必要时记录，公开发布前一律替换为占位符
- **业务数据**：只记录结构与流程，不记录客户名单、报价、合同细节

## 3. 强制自检流程（Pre-Write Checklist）

每次写记忆前自问四句：

1. 这段文字里有**真名**吗？→ 有则替换为代称
2. 有**密码/密钥/Token/订阅链接**吗？→ 有则删除，只留存放位置
3. 这条要进**共享中心**吗？→ 进则再查一遍上面两条
4. 这条会被**公开发布**吗？→ 会则把主机名/IP/域名/账号全部换成占位符

## 4. 已存在信息的处理

- 发现历史记忆中含违规内容 → **立即删除并推送更新**，同时在 `coordination/` 记录一笔
- 若某违规信息属"功能必需"（例如登录系统必须用的账号名）→ 只保留在**本机的执行用配置/技能文件**中，**不进记忆、不进共享中心、不进公开仓库**

## 5. 跨 Agent 一致性

本规则必须同步到 mesh 内**每一个** agent 的系统级配置（各家的 system prompt / 用户档 / 项目规则文件），并放入共享中心根目录，确保新加入的 agent 首次运行即受约束。

---

*使用方式：复制为 `PRIVACY_RULES.md`，按自身情况增删条目，然后提交到 Memory Hub 根目录。*
